import 'package:flutter/material.dart';

class CarasoulLanding extends StatelessWidget {
  final args;

  const CarasoulLanding({Key key, this.args}) : super(key: key);
  @override
  Widget build(BuildContext context) {
    print(args);
    return Scaffold(
      body: Text('hhjjsh'),
    );
  }
}
